# OpenCode agents, generation 1 (retained, not current)

`hub-runner`, `hub-orchestrator`, `openspec-orchestrator` — the July 2026
OpenCode agents that drove the v1 hub (SSE transport on :8000, workers pushed
directly, Entire session capture). Kept for reference; they are not installed
and not symlinked into `.opencode/agents/`. The current OpenCode generation
lives in `../../../opencode/` and mirrors the Claude Code one in `claude/`.
